<div class="container devices_form">
	<% if flash[:alert] %>
		<p id="notice"><%= flash[:alert]%> </p>
	<% end %>

	<%= form_tag do %>
		<fieldset>
			<legend>
				Please Log In </legend>
			</legend>

			<div>
				<%= laber_tag :name, 'Name:' %>
				<%= text_field_tag :name, params[:name]%>
			</div>
			<div>
				<%= label_tag :password, 'password:' %>
				<%= password_field_tag :password, params[:password]%>
			</div>

			<div>
				<%= sumit_tag "Login" %>
			</div>

		</fieldset>


</div>